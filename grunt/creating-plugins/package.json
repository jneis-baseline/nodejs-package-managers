{
  "name": "creating-plugins",
  "version": "1.0.0",
  "description": "creating grunt plugins - scaffolding new-plugin project",
  "devDependencies": {
    "grunt": "^0.4.5"
  },
  "scripts": {
    "install-init": "npm install -g grunt-init",
    "clone-template": "git clone git://github.com/gruntjs/grunt-init-gruntplugin.git ~/.grunt-init/gruntplugin",
    "init-plugin": "rm -r new-plugin && mkdir new-plugin && cd new-plugin && grunt-init gruntplugin"
  }
}
